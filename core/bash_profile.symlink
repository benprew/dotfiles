# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs

unset USERNAME

export PATH="/usr/local/heroku/bin:$PATH:/usr/local/share/npm/bin:$HOME/bin:/usr/local/Cellar/ruby/2.0.0-p0/bin"
