# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs

PATH=/usr/local/Cellar/ruby/1.9.3-p194/bin:$PATH:$HOME/bin

export PATH
unset USERNAME

export PATH="/usr/local/heroku/bin:$PATH:/usr/local/share/npm/bin"
